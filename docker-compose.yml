services:
  app:
    build: .
    container_name: internal-kernexa-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=${POSTGRES_DB}
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kernexa-net

  db:
    image: postgres:16
    container_name: internal-kernexa-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - kernexa-data1:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - kernexa-net

  adminer:
    image: adminer
    container_name: internal-kernexa-db-adminer
    restart: unless-stopped
    command: php -S 0.0.0.0:8080 -t /var/www/html   
    ports:
      - "8060:8080"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kernexa-net

networks:
  kernexa-net:

volumes:
  kernexa-data1:

